#include<stdio.h>
#include<stdlib.h>
#include<time.h>

/**
 * main - Entry point
 * Return; always 0 (sucess)
 */

int main(void)
{

	char low;

	for  (low = 'a'; low <= 'z'; low++)
	putchar9low);

	for (low = 'a'; low <= 'Z'; low++)
	putchar(low);
	putchar('\n');

	return (0);

}
